#include <iostream>
using namespace std;

int main(){

    // array of 100 integers
    // filled with garbage values
    int marks[100]; 

    // array of 100 integers
    // first 3 elements are initialized
    // rest of the elements are filled with 0
    int marks1[100] = {1,2,3}; 

    // array of 3 integers
    // all elements are initialized
    int marks2[] = {1,2,3}; 

    return 0;
}